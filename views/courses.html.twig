<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <meta charset="utf-8">
        <title>Registrar's Office</title>
    </head>
    <body>
        <div class="container">
            <h1>Courses</h1>
            {% if courses is not empty %}
                <h3>Here are your courses!</h3>
                {% for current_course in courses %}
                    <a href="courses/{{current_course.getId}}"><p>{{current_course.getName}}</p></a>
                {% endfor %}
            {% endif %}
            <form action="/courses/delete" method="post">
                <button type="submit">Delete All Courses</button>
            </form>
        </div>
    </body>
</html>